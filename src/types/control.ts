export enum ControlMode {
    TRACKPAD = 'TRACKPAD',
    KEYBOARD = 'KEYBOARD'
}

export interface MouseData {
    dx: number;
    dy: number;
}

export interface ScrollData {
    dy: number;
}

export enum MouseButton {
    LEFT = 'LEFT',
    RIGHT = 'RIGHT',
    MIDDLE = 'MIDDLE'
}


